// dynamic

let name = "value";
const konstante = 42;


// kommandostrukturen

if (konstante == 42) {
  // wenn true
} else {
  // wenn false
}

// arras, maps

let array = [1, 2, 3];
array.push(4);
array.push("4");

let maps = new Map();
maps.set("key", "value");
maps.get("key");

for (let i of array) {
  console.log("wert: " + i);
  console.log()
}
